{
  "name": "kapsel",
  "private": true,
  "version": "0.0.0",
  "description": "Kapsel protocol monorepo — spec, SDK, CLI, registry, and examples",
  "license": "Apache-2.0",
  "author": "Joeybuilt LLC",
  "repository": {
    "type": "git",
    "url": "https://github.com/joeybuilt-official/kapsel"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.6.0",
    "eslint": "^9.0.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0"
  }
}
